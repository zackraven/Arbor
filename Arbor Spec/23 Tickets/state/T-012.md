---
status: done
---

## Blocked

## Implementation notes

### Files created
- `src/state/tree-store.ts` — Zustand store with `selectedTreeId` / `selectTree` as specified.
- `src/progress-ring.tsx` — SVG progress ring; reads `tokens.progressRing.size` and `tokens.progressRing.strokeWidth` from contracts; renders track/fill circles and centred completed/total text using CSS custom properties only.
- `src/progress-ring.module.css` — styles using `var(--color-text-primary)` for text fill; no hardcoded colours.
- `src/tree-list.tsx` — home view; non-Tauri mode builds one mock `TreeSummary` from the fixture (node_count from actual fixture length, completed_count fixed at 5); Tauri mode calls `listTrees()`; renders cards with `data-testid="tree-card"`.
- `src/tree-list.module.css` — card layout using CSS custom properties; max-width 600px (raw layout value, not a colour/spacing token).
- `src/graph/summary-panel.tsx` — side panel; reads selected node synchronously from the graph store for immediate title/one_liner/category/status render; loads outcomes async from fixture (non-Tauri) or `getNode()` (Tauri); shows children from graph store edges.
- `src/graph/summary-panel.module.css` — panel styles; background `var(--color-surface)`, border `var(--color-border)`, transition `transform var(--motion-duration-normal) var(--motion-easing)`; status badge classes for each unlock state.
- `src/app.module.css` — app-level viewport layout; back button styles; no hardcoded colours.

### Files modified
- `tsconfig.json` — removed `"tests/T-012"` from the `exclude` array.
- `src/App.tsx` — conditional rendering: `selectedTreeId === null` → `<TreeList />`; selected → `<GraphView treeId={selectedTreeId} />` with back button. Keeps `document.title = 'Arbor'` for T-001 smoke test.
- `src/graph/graph-view.tsx` — added `import SummaryPanel` and `<SummaryPanel />` after `<ReactFlow>`.
- `src/graph/graph-view.module.css` — added `position: relative` to `.container`.
- `src/graph/arbor-node.tsx` — added `data-testid="arbor-node"` to the outer node div.

### Nits not acted on
- The `summary-panel.tsx` initially attempted to load all node details via a `NodeDetail` object asynchronously, causing the "renders node details" test to see "Loading…" instead of the title. Restructured to read title/one_liner/category synchronously from the graph store and only fetch outcomes asynchronously — this satisfies both the synchronous render test and the async outcomes display.

## Verification

Verification: pass — 2026-08-07
- tests/T-012/summary-panel.test.tsx: passed (3/3 tests)
- tests/T-012/tree-list.test.tsx: passed (4/4 tests)
- tests/T-009/token-lint.test.ts: passed — no hardcoded colours introduced
- Full test suite: 102/102 tests passed across 11 test files
- pnpm lint: exits 0 (tsc --noEmit clean, cargo clippy clean)
- pnpm observe --route / --ticket T-012 --actions tests/T-012/actions.json --out .claude/session-logs/T-012-observe: produced 4 non-empty PNG screenshots (T-012-00-initial.png 12549B, T-012-01-tree-list.png 12549B, T-012-02-graph-view.png 148733B, T-012-03-summary-panel.png 147594B)
- Integrity check: no protected paths (contracts/, Arbor Spec/21 Contracts/, tests/T-NNN/*.test.ts, .claude/) in diff
- Out-of-scope: no violations — contracts/tokens.ts, contracts/commands.d.ts, src/tokens.css, src/graph/layout-engine.ts, src/state/graph-store.ts all unmodified; no test files edited; no routing library added
