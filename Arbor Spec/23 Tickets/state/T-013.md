---
status: done
---

## Blocked

## Implementation notes

### Files modified

**`src/tokens.css`**
- Updated all colour custom properties to light theme values (base: `#f5f5f5`, surface: `#ffffff`, etc.)
- Added `--color-node-outline: #333333`
- Replaced `--node-width: 180px`, `--node-min-height: 50px`, `--node-border-radius: 8px` with `--node-diameter: 80px`
- Added `--node-label-font-size: 10px` and `--node-label-line-height: 1.3`
- Updated `--graph-edge-color`, `--graph-background`, `--progress-ring-track-color`, `--progress-ring-fill-color` to light theme values

**`src/graph/layout-engine.ts`**
- Added `import { tokens } from '../../contracts/tokens'`
- Replaced hardcoded ELK `layoutOptions` with values from `tokens.elk` (algorithm, direction, nodeSpacing, layerSpacing, edgeRouting, crossingMinimization, crossingMinimizationThoroughness)
- Added y-coordinate flip after `elk.layout()` returns, conditioned on `tokens.elk.yFlip`

**`src/graph/arbor-node.tsx`**
- Removed `oneLiner` from destructuring and JSX display
- Replaced `.title` + `.oneLiner` spans with a single `.label` span inside the circle node
- `oneLiner` field retained in `ArborNodeData` interface (used by T-011/T-012)

**`src/graph/arbor-node.module.css`**
- Replaced `.title` and `.oneLiner` classes with `.label` class
- `.node` restyled: `width/height: var(--node-diameter)`, `border-radius: 50%`, `flex` centering
- `.label` applies `var(--node-label-font-size)`, `var(--node-label-line-height)`, `-webkit-line-clamp: 3`

**`src/graph/graph-view.tsx`**
- Changed `tokens.node.width` → `tokens.node.elkWidth` (line 29)
- Changed `tokens.node.minHeight` → `tokens.node.elkHeight` (line 30)
- Changed edge `type: 'smoothstep'` → `type: 'straight'`

**`src/graph/graph-view.module.css`**
- No changes needed; already uses `var(--color-base)` which now resolves to the light theme value

### Nits not acted on

- The post-edit-lint hook caught the `tokens.node.width` / `tokens.node.minHeight` errors on first write of `layout-engine.ts`; fixed in the same session before moving to subsequent steps.
- T-011 and T-012 tests fail due to unimplemented future tickets — these are pre-existing and out of scope for T-013.

## Verification

Verification: pass — 2026-08-07
- Integrity check: `git diff --name-only HEAD` shows only `src/tokens.css`, `src/graph/layout-engine.ts`, `src/graph/arbor-node.tsx`, `src/graph/arbor-node.module.css`, `src/graph/graph-view.tsx`, and the sidecar. No protected paths.
- tests/T-010/graph-render.test.tsx: passed (5/5)
- tests/T-009/token-lint.test.ts: passed (2/2)
- pnpm lint: exits 0 (tsc --noEmit clean, cargo clippy clean)
- pnpm observe --route / --ticket T-013 --out .claude/session-logs/T-013-observe: produced non-empty screenshot PNG (140757 bytes at `.claude/session-logs/T-013-observe/T-013-00-initial.png`)
- Out-of-scope: no violations — no new files, no test edits, no contracts/tokens.ts modification, no state/graph-store.ts or use-graph-loader.ts modification, no dark theme, no click/panel logic
- Contract C7 token values in src/tokens.css match contracts/tokens.ts exactly for all 16 colour properties, node dimensions, graph properties, and progress ring properties
