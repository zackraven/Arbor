---
status: done
---

## Blocked

## Implementation notes
Rework (2026-07-31): fix infinite-recursion in beforeDevCommand. `tauri.conf.json` had `beforeDevCommand: "pnpm dev"` and `package.json` had `"dev": "tauri dev"`, forming a loop. Fix: changed `beforeDevCommand` from `"pnpm dev"` to `"vite"` and `beforeBuildCommand` from `"pnpm build"` to `"vite build"`.

## Verification
Verdict: pass — 2026-07-31
