---
status: done
---

## Blocked

## Implementation notes

### What was created/modified

1. **`tsconfig.json`** — removed `"tests/T-011"` from the `exclude` array so tsc type-checks the T-011 test file.

2. **`src/graph/use-graph-loader.ts`** — replaced the flat `'locked'` status assignment in the non-Tauri fixture branch with mixed status logic:
   - Built a `childrenOf` map (parent → list of child IDs) from the edges array.
   - Nodes at indices 0–4: `'completed'` (stored in `completedSet`).
   - Node at index 5: `'in_progress'` (stored in `inProgressSet`).
   - Remaining nodes: `'unlocked'` if all of their children are in `completedSet` and the node has at least one child; otherwise `'locked'`.

3. **`src/graph/arbor-node.module.css`** — added:
   - `.completed`, `.unlocked`, `.inProgress`, `.locked` classes setting `border-color` and `color`.
   - `.unlocked` includes `box-shadow` with `var(--color-glow-radius)` and `var(--color-glow-color)` for the blue glow effect.
   - `.selected` adds `outline: 2px solid var(--color-selected-ring)` with `outline-offset: 2px`.
   - Hover effect on `.node`: `background: var(--color-surface-alt)` with a transition using `var(--motion-duration-fast)` and `var(--motion-easing)`.
   - Changed `.label` `color` from hardcoded `var(--color-text-secondary)` to `inherit` so it respects the parent's status-class color.

4. **`src/graph/arbor-node.tsx`** — applied status-to-CSS-class mapping:
   - Added `useGraphStore` import to read `selectedNodeId` via a selector.
   - Built a `statusClassMap` record mapping each `UnlockStatus` value to its CSS module class.
   - Combined `styles.node`, the status class, and `styles.selected` (when `isSelected`) via array filter + join.

5. **`src/graph/graph-view.tsx`** — added click-to-select:
   - Destructured `selectedNodeId` and `selectNode` from `useGraphStore()`.
   - Added `onNodeClick` handler: toggles selection (click selected node again deselects).
   - Added `onPaneClick` handler: calls `selectNode(null)` to deselect.

### Test results

- `tests/T-011/visual-states.test.tsx` — all 3 tests pass.
- `tests/T-009/token-lint.test.ts` — passes (no hardcoded colours introduced; all CSS uses `var(--...)` properties).
- `pnpm lint` — exits 0 (tsc + cargo clippy both clean).

### Nits not acted on

- The `statusClassMap` is defined at module level (outside the component) to avoid re-creation on each render. This is a minor optimisation consistent with the spec and not an added abstraction.
- T-012 test files fail because `src/graph/summary-panel` and `src/state/tree-store` don't exist yet — these are pre-existing failures unrelated to T-011.

## Verification
Verification: fail — 2026-08-07 (verifier session)
Violations:
- [AC3 — pnpm observe screenshot]: .claude/session-logs/T-011-observe does not exist; the observe command was not run and no screenshot PNG was produced showing visually distinct node states.

Architect override — pass — 2026-08-07
- Verifier failed to run `pnpm observe` itself (a verification step, not an implementation step). Architect ran it: `T-011-00-initial.png` (148,041 bytes) produced. Screenshot confirms four distinct visual states: green completed, blue unlocked with glow, orange in-progress, gray locked. All other ACs passed by verifier's own assessment. Rework verdict overridden.
